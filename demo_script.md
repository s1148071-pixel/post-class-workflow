---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 4d07511ad5ce88409689fe850a421ffa_1ec6d43d647311f196be5254006c9bbf
    ReservedCode1: D3mpedmoO90v/dYBDBiVDm34mvLGk8GNb/Dad7TKpgQroYaVDMlJDPGiOii4vcxqvx7VGstdgOkwyBk9YnVd26rOrofjKcXw667tQSYHcjJxhFL3oP72+iM3E9SkVt7aXea79YQo54+DxmvpOSYHaJ8ly2w0b4bu8bsSS1SHe56oWQN6GK/yAHP0+T4=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 4d07511ad5ce88409689fe850a421ffa_1ec6d43d647311f196be5254006c9bbf
    ReservedCode2: D3mpedmoO90v/dYBDBiVDm34mvLGk8GNb/Dad7TKpgQroYaVDMlJDPGiOii4vcxqvx7VGstdgOkwyBk9YnVd26rOrofjKcXw667tQSYHcjJxhFL3oP72+iM3E9SkVt7aXea79YQo54+DxmvpOSYHaJ8ly2w0b4bu8bsSS1SHe56oWQN6GK/yAHP0+T4=
---

# 课后输出工作台 — Demo 汇报稿

> 汇报人：[待填]  
> 时间：2026年6月10日 下午2:00  
> 时长：约 8-10 分钟  

---

## 一、设计思路

### 1.1 问题背景

英语老师每节课后有三件必做但耗时的事：

1. 写**课后反馈报告**——总结课堂内容、评估学生表现、给家长建议
2. 找**学生高光片段**——从录屏中挑出精彩发言，剪辑分享
3. 出**课后练习作业**——基于当堂词汇设计游戏化练习，让学生愿意做

这三件事全部手工做，一节课至少 40 分钟。我们做这个项目的目标很明确：**老师上完课，粘贴文章和词汇，AI 三件事一起做，五分钟搞定。**

### 1.2 整体架构：四阶段状态机

```
提交素材  →  AI 处理  →  审核编辑  →  发布下载
(1 min)     (2 min)     (2 min)     (10s)
```

设计上刻意把流程分成四个不可跳跃的阶段，每个阶段对应 Streamlit 的一个 `session_state["stage"]`。这不是为了炫技——真实的老师使用场景里，**提交后必须看到 AI 在工作（不是黑盒等待），AI 产出后必须先审再发（不能直出）**。四阶段设计对应的是这两个真实需求。

### 1.3 三层分离的工程结构

```
prompts.py    → 构建 LLM messages（调整 Prompt 只改这一个文件）
llm_client.py → 调用 DeepSeek API + 解析 JSON + fallback 降级
app.py        → 渲染 Streamlit UI，不碰 Prompt 也不碰 API
```

这样分层的好处是：改 Prompt 不用翻 UI 代码，加新模型不用动界面，调试时问题定位清晰。Demo 阶段可能感受不到，但后续迭代会很受益。

### 1.4 两条最重要的设计原则

**原则一：Demo 永远不能挂。** 整套系统有三层保障——Mock 数据预置、API 调用失败自动降级到 Mock、`test_game_data.py` 提供 12 项自动化检测。没有 API Key、网络断了、LLM 返回格式错乱，系统都会自动切到 Mock 模式，审核台会显示"Mock 降级"标记，但整套流程照跑。

**原则二：游戏作业的质量靠模板内建修复保证。** 之前交付过 11 款英语教学游戏，踩了 14 个已知 bug（流星保底计数器缺失、捕梦网字母匹配错误、星座连线重复判定等），这次全部在模板层 baked-in 修复。新生成的游戏继承这些修复，不需要每次都重新踩坑。`test_game_data.py` 的 12 项检测里包含了这些 bug 的回归检查。

---

## 二、当前开发效果

### 2.1 提交阶段：极简输入

打开页面，填三个东西：老师姓名、学生年级（下拉选择）、文章内容（粘贴）、词汇表（英文+中文释义，每行一个）。录屏可以上传也可以跳过，Demo 用 Mock 数据演示。输入完点"开始 AI 处理"，进入处理管线。

设计上有一个细节：**快速 Demo 按钮**——点一下自动填充 14 个词汇和一篇短文，演示时不需要现场打字。

### 2.2 AI 处理阶段：让等待变成体验

很多 AI 产品的处理阶段就是一个进度条转圈圈，用户不知道 AI 在干嘛。我们做了两件事让等待变得可感知：

**第一，19 个微阶段滚动日志。** 从初始化管线 → ASR 语音识别（3 个阶段）→ 反馈报告生成（6 个阶段）→ 高光片段识别（3 个阶段）→ 游戏作业生成（7 个阶段），每一步都有终端风格的日志面板实时滚动。"调用 DeepSeek v4-pro Chat API… 等待 LLM 响应（预计 30-60s）……response received（2.8k tokens）"——这些信息让用户知道系统在做什么、做到哪了。

**第二，三任务并行调用。** 反馈报告约 40 秒、游戏作业约 100 秒，两项 LLM 调用是并行的，总时间取最长，不是串行相加。

**第三，旋转加载动画。** 双环旋转（teal + amber），中心图标在报告/视频/游戏三个图标之间循环切换，视觉上暗示"三件事同时在跑"。

实际点击"开始 AI 处理"到"进入审核台"，全程约 2 分钟（含 LLM 响应时间），用户看到的是持续滚动的日志面板。

### 2.3 审核编辑台：三栏仪表盘

这是整个产品的核心交互界面。左中右三栏，对应三个 AI 产物：

**左栏 — 反馈报告：**
- 编辑模式 / 预览模式一键切换
- 逐段可展开编辑：课堂摘要、学生评语、教学建议、家长指引
- 每个编辑区下方显示修改状态："已修改 — 共 128 字（原文 112 字）"，附带"查看 AI 原文"折叠面板
- 勾选"审核完毕"后显示修改摘要：统计每个字段的增删字数

**中栏 — 视频高光剪辑：**
- AI 自动推荐的片段列表，每条带评分（百分比 badge，绿色 ≥ 90%、琥珀色 ≥ 70%、蓝色 < 70%）、学生姓名、时间戳
- 展开查看推荐理由和检测信号（教师正面反馈、发言时长达标、LLM 语义评分）
- 老师可以取消勾选或手动添加新片段
- Demo 阶段视频数据是 Mock 的，但 UI 逻辑完整

**右栏 — 课后游戏作业：**
- 10 关游戏机制预览，可展开查看每关的答案、选项、干扰项
- 支持逐关删除
- 展开故事预览（中英对照段落）
- 展开词汇表（14 个词，含 emoji）

三栏末尾都有独立的"审核完毕"勾选框，全部勾选后方可发布。这样可以防止漏审。

### 2.4 课后游戏：10 关固定机制

游戏是单文件 HTML，零外部依赖，学生手机直接打开就能玩。10 关机制是固定的：

| 关卡 | 机制 | 技能 |
|:--:|------|------|
| 1 | 气球挑战 | 词义匹配 |
| 2 | 暗夜搜寻 | 图标识别 |
| 3 | 情景辨析 | 看图选词 |
| 4 | 视觉拼写 | 补全单词 |
| 5 | 字母拼图 | 按序拼词 |
| 6 | 记忆配对 | 图文匹配（塔罗牌风格） |
| 7 | 接流星 | 快速识别 |
| 8 | 星座连线 | 拼写顺序 |
| 9 | 捕梦网 | 拖拽拼词 |
| 10 | 刮刮卡 | 综合回顾 + 情景判断 |

AI 要做的事不是设计关卡机制（机制是固定的），而是**基于当堂课文章和词汇，为每个关卡出题**——匹配正确的 emoji、设置干扰项、编排故事段落。这样既能保证游戏质量（机制经过验证），又能做到内容个性化（每节课的题目不同）。

发音采用有道 dictvoice API，游戏内所有单词点击即可听发音。

### 2.5 发布阶段与质量保障

发布后三样产出物可下载：反馈报告 `.txt`、高光剪辑列表 `.json`、游戏 HTML 文件。游戏 HTML 还附带 JSON 原始数据兜底。

质量保障方面，`test_game_data.py` 提供独立的命令行验证工具，12 项检测覆盖：数据层完整性、词汇覆盖率（确保每个词至少作为答案出现一次）、HTML 静态分析（DOM 元素、关键函数、Bug 回归检查）。可以脱离 Streamlit 单独运行，适合 CI 集成。

### 2.6 视觉设计

设计系统叫"温润编辑部"——暖纸底色（FCFAF7）、teal 主色（206F6B）、amber 强调色（D48C3C）。灵感来自纸质编辑台，强调专业感和可读性。进度条用四阶段圆点连线，处理阶段用深色终端风格日志面板（#0D1117 背景 + Cascadia Code 等宽字体），审核阶段用三栏卡片布局。响应式适配移动端，最小宽度 640px 下有降级处理。

---

## 三、后续开发计划

### Phase 4：真实视频处理（待开发）

目前视频高光片段用的是 Mock 数据。后续接入真实 ASR（Whisper）做语音转文字 + 说话人分离，再用 FFmpeg 按时间戳自动剪辑。处理管线里已经预留了 ASR 的三个微阶段（加载声学模型 → 说话人分离 → 转写输出），只需要把 Mock 替换为真实调用。

### Phase 5d：背景图生成（待接入）

游戏模板目前使用 CSS 渐变作为背景。后续接入生图 API，根据当堂课主题自动生成背景图，让每节课的游戏视觉风格都不一样。

### Phase 6：内部试用

找 2-3 位英语老师实际使用，收集以下反馈：
- 提交流程是否顺畅（老师愿意填多少信息？录屏是不是必须的？）
- AI 产出质量是否达标（反馈报告是否贴切？游戏难度是否匹配年级？）
- 审核编辑台是否好用（还需要哪些编辑能力？）
- 学生是否愿意玩课后游戏（完课率、正确率统计）

### 中期探索方向

- **学生端小程序**：学生扫码即玩，成绩回传老师
- **多节课对比**：同一学生跨课表现趋势图
- **语音互动**：游戏内加入跟读评分（发音评测 API）
- **国际化**：Prompt 和 UI 的多语言支持，扩展到其他语种教学

---

## 附录：技术指标速览

| 维度 | 数据 |
|------|------|
| 处理阶段 | 4 阶段状态机 + 19 微阶段日志 |
| 管线耗时 | 约 2 分钟（含 LLM 响应） |
| LLM 模型 | DeepSeek v4-pro |
| 游戏机制 | 10 关固定类型 |
| 已知 Bug 修复 | 14 个 bake-in |
| 自动化检测 | 12 项（数据 + HTML） |
| 技术栈 | Streamlit 1.57 + DeepSeek API + 原生 HTML/CSS/JS |
| 核心文件 | app.py（1688 行）+ utils 模块 + 游戏模板 |

---

*本汇报稿基于项目文件（README.md、app.py、test_game_data.py、test_game_v3.html）撰写。*
*（内容由AI生成，仅供参考）*
